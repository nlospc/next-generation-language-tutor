# Phase 0: 项目意图固化

## 目标

把现在讨论出来的方向固化成项目内设计文件，作为后续所有 agent 的主 design intent。

## 交付

- 新增 `docs/design-intent.md`
- 新增 `docs/implementation-slices.md`
- 明确：
  - agent-first
  - provider/gateway neutral
  - 任意语言
  - MCP first
  - Web UI as local private console
  - learning methods for both user and agent
  - daily check-in calendar
  - Python MVP is legacy reference only

## 验证

- 文档能让另一个 agent 不看聊天记录也理解目标。
- 用户确认设计意图没有偏离。
