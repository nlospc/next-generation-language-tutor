# Getting Started

This private language tutor runs locally.

- Run `npm run migrate` to initialize local data.
- Run `npm run serve:web` to open the web console.
- Run `npm run serve:mcp` to expose MCP tools for agents.

Use the same content as both user docs and agent context.
